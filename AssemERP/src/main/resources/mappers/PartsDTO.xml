<!-- <?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.WiseForce.AssemERP.PartsDTOMapper">

	<select id="shPartsListPage" parameterType="PartsDTO" resultType="PartsDTO">
		select *
		From (
				select rownum rn, a.*
				From
					(SELECT *
					 FROM parts
					 ORDER BY parts_no) a
			 )
		WHERE rn BETWEEN #{start} and #{end}
	</select>

</mapper>
 -->